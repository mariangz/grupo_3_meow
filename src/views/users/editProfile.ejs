<!DOCTYPE html>


<html lang="es">

<head>
    <%- include('../partials/head', {pageTitle: 'Meow!'}); %>
        <link rel="stylesheet" href="/css/productDetail.css">
</head>

<body>
    <%- include('../partials/header'); %>
        <main>
            <div class="form-container">
                <h1>Editar Mi Perfil</h1>
                <div class="field">
                    <img src="/images/users/<%= user.image %>" width="50%" align-item="center">
                </div>

                <form action="/users/editProfile/<%=user.user_id %>?_method=PUT" method="POST">
                    <div class="field">
                        <label for="name">
                            <span>Nombre completo</span>
                            <input type="text" name="name" id="name" autofocus value=<%= user.name %>>
                        </label>
                    </div>
                    <div class="field">
                        <label for="email">
                            <span>Email</span>
                            <input type="email" name="email" id="email" autofocus value=<%= user.email %>>
                        </label>
                    </div>
                    <div class="field">
                        <label for="password">
                            <span>Contraseña:</span>
                            <input type="password" name="password" id="password" autofocus value=<%= user.password %>>
                        </label>
                    </div>
                    <div class="field">
                        <label for="confirmar-contraseña">
                            <span>Contraseña:</span>
                            <input type="password" name="confirmPassword" id="confirmar-contraseña" autofocus value=<%= user.confirmPassword %>>
                        </label>
                    </div>
                    <!-- <div class="field">
                        <div class="file-select">Cambiar la imagen de Perfil:
                            <div class="upload-image__label">
                                <label class="subirImagen" for="userAddImage">
                                    <i class="fas fa-upload icon-upload"></i>Click aquí</label>
                            </div>
                            <input type="file" name="image" id="userAddImage" accept=".jpg, .jpeg, .png, .gif">
                            <% if ( locals.errors && errors.image) { %>
                                <p class="text-danger">
                                    <%= errors.image.msg %>
                                </p>
                                <% } %>
                        </div>
                    </div> -->
                    <div class="button-product">
                        <button type="submit" class="button">Guardar cambios</button>
                    </div>
                </form>
            </div>
        </main>
        <%- include('../partials/footer'); %>
</body>